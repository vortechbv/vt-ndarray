vt::ndslice::element_count
==========================

```c++
constexpr std::size_t element_count() const noexcept;
```

Returns the total number of elements in the slice.

Return value
------------

The total number of elements in the slice.
